realm test_backend.branch_compare

fn delta(left: Int32, right: Int32) -> Int32 {
    when left > right {
        give left - right
    } otherwise {
        give right - left
    }
}

fn main() -> Int32 {
    give delta(9, 4)
}
