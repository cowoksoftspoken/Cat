# Claw Docs

Internal notes for the current revised migration of the compiler.

Files:
- `builtin-methods.md`
  Tracks builtin method dispatch that is already implemented, plus the naming direction for the revised surface.
- `frontend-safety-status.md`
  Summarizes what safety guarantees the frontend already enforces and what is still missing.
- `roadmap-next.md`
  Captures the next implementation order so migration work stays deliberate and does not drift.
