shape Cache {
    stored: Anchor[Str]
}

fn make_cache(value: Str) -> Cache {
    return Cache(Anchor.new(value))
}

fn get(cache: ref Cache) -> ref Str {
    return cache.stored.get()
}

fn main() {
    val my_cache = make_cache("hello")
    val content = get(my_cache)
}
