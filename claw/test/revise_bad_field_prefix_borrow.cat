shape Db {
    host: Str
    port: Int32
}

shape Config {
    db: Db
}

fn bad(cfg: Config) {
    var local = cfg
    val db: ref Db = ref local.db
    val port: ref mut Int32 = ref mut local.db.port
}
