realm test.type_infer_slot_diagnostics

fn conflicting_assignment_types() {
    slot value
    value = 1
    value = "oops"
}

fn invalid_view_reassignment(text: look Text) {
    slot prefix
    prefix = text.slice(0, 1)
    prefix = 1
}
