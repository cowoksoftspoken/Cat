choice Result[T, E] {
    Ok(value: T)
    Fail(cause: E)
}

fn step(flag: Bool) -> Result[Int32, Str] {
    if flag {
        return Ok(7)
    }
    return Fail("bad")
}

fn bad(flag: Bool) -> Result[Int32, Str] {
    var value = try step(flag)
    return Ok(value)
}
