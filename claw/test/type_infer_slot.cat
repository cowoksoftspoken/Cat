realm test.type_infer_slot

fn infer_from_first_assignment() {
    slot count
    count = 1
    print(count)
}

fn infer_view_from_first_assignment(text: look Text) -> look Text {
    slot prefix
    prefix = text.slice(0, 1)
    give prefix
}
