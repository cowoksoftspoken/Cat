shape Db {
    host: Str
    port: Int32
}

shape Config {
    db: Db
}

fn allow(cfg: Config) {
    var local = cfg
    val host: ref Str = ref local.db.host
    val port: ref mut Int32 = ref mut local.db.port
    println(host.len())
}
