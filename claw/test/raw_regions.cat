realm test.raw_regions

fn bridge(flag: Int32) -> Int32 {
    slot value: Int32 = 0
    raw {
        when flag > 0 {
            value = flag
        } otherwise {
            value = 1
        }
        print(value)
        println("raw bridge")
    }
    give value
}
