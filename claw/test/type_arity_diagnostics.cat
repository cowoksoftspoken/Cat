realm test.type_arity_diagnostics

shape Box of T {
    share item: T
}

choice Maybe of T {
    none
    some(value: T)
}

fn broken(a: Box, b: Maybe of Int32, Text, c: Text of Int32, data: Span, items: Vec, pairs: Table of Text) {
    give
}