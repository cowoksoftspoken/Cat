realm test.safety_diagnostics

fn bad_edit(text: edit Text) -> edit Text {
    give text
}

fn leak_local() -> look Text {
    slot message: Text = "hello"
    give message
}

fn raw_outside() {
    hold ptr: RawPtr of Byte
}
