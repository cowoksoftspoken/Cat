shape Db {
    host: Str
    port: Int32
}

shape Config {
    db: Db
}

fn allow(cfg: ref mut Config) {
    val host: ref Str = ref cfg.db.host
    val port: ref mut Int32 = ref mut cfg.db.port
    println(host.len())
}
